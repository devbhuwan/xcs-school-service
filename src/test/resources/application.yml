spring:
  application:
    name: school-service
---
spring:
  profiles: cucumber
  data:
    cassandra:
      keyspace-name: sample
      port: 9142
  cloud:
    config:
      enabled: false
security:
  basic:
    enabled: false